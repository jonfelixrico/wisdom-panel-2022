server {
    listen       80;
    server_name  localhost;

    location /api/ {
      proxy_pass ${API_BASE_URL}/;
    }

    location / {
      root   /usr/share/nginx/html;
      index  index.html index.htm;
    }
}
